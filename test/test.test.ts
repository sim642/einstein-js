import "mocha";
import {assert} from "chai";

describe("Test", function () {
    it("should succeed", function () {
        assert(true);
    });

    it("should fail", function () {
        assert(false);
    });
});